import {View, Text} from 'react-native';
import React from 'react';
import {showModal, hideModal} from '../redux/store';
import {useSelector} from 'react-redux';
import myConstants from '../config/constants';

const DataModal = () => {};

export default DataModal;
